# User: Questions
project_name:
  type: str
  help: Project name

python_version:
  type: str
  help: Python version
  choices:
    - ["3.11.5", "Python 3.11.5 (Cloudflare compatible)"]

docker_image_base:
  type: str
  help: Docker base image
  choices:
    - ["python", "Python"]

docker_image_flavour:
  type: str
  help: Docker image flavour
  choices:
    - ["slim-bookworm", "Debian Bookworm Slim"]
