# User: Questions
project_name:
  type: str
  help: Project name

python_version:
  type: str
  help: Python version
  choices:
    - value: "3.11.5"
      label: "Python 3.11.5 (Cloudflare compatible)"

docker_image_base:
  type: str
  help: Docker base image
  choices:
    - value: "python"
      label: "Python"

docker_image_flavour:
  type: str
  help: Docker image flavour
  choices:
    - value: "slim-bookworm"
      label: "Debian Bookworm Slim"
